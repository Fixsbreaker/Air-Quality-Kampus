# Необязательная зависимость: Prophet ставится отдельно, потому что тянет
# компилятор Stan и заметно замедляет сборку образа.
#   pip install -r requirements-prophet.txt
# Если библиотека не установлена, app/ml/prophet_model.py корректно
# сообщает об этом, а обучение продолжается на LightGBM.
prophet==1.1.6
